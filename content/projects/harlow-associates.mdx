export const metadata = {
  title: "Harlow & Associates",
  slug: "harlow-associates",
  order: 1,
  industry: "Law Firms",
  stack: ["WordPress", "ACF", "Gravity Forms", "Tailwind"],
  summary:
    "A custom WordPress rebuild for a mid-size law firm, replacing a template-based site with a fast, lead-focused platform built to convert case inquiries.",
  thumbnail: "https://picsum.photos/seed/lawfirmhero/1400/620",
  liveUrl: "#",
  codeSnippet: {
    filename: "functions.php — gravity-forms-routing.php",
    code: `// Route intake submissions by selected practice area
add_filter('gform_notification', function( $notification, $form, $entry ) {
    $practice_area = rgar( $entry, '6' );
    $routing = get_field( 'practice_area_routing', 'option' );

    if ( isset( $routing[ $practice_area ] ) ) {
        $notification['to'] = $routing[ $practice_area ];
    }

    return $notification;
}, 10, 3 );`,
  },
}
